{
  "name": "pysopstrategy",
  "version": "0.1.0",
  "description": "Deterministic dip shield planning and detection (policy-only).",
  "main": "src/cli.js",
  "scripts": {
    "test": "jest --runInBand",
    "simulate": "node src/cli.js simulate",
    "lint": "echo 'no linter configured'"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "axios": "^1.4.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "jest": "^29.0.0"
  }
}